services:
  - type: web
    name: laravel-app
    env: php
    buildCommand: |
      composer install
      npm install
      npm run build
      php artisan key:generate
    startCommand: php artisan serve --host=0.0.0.0 --port=10000